We're maintaining a fork of fabro-sh/fabro at fkukuck/fabro. in this fork, we mainly maintain an Azure compatibility. Our fork has diverged from origin/main and we now want to rebase onto it. i've already done `git fetch upstream` so upstream/main is up to date. i want you to rebase the current branch onto upstream/main and resolve all conflicts. moreover, within this process i would like you to check the changes (diffs) the main branch performed since our two branches diverged, and then compare with what we've done in our current checked out Azure branch. if there is anything by now in the upstream/main that we originally did ourselves, of course align with the upstream solution. And of course keep existing functionality working under the potentially new way of doing things in the updated branch.
One additional thing I ask you to consult is `docs/public/changelog/`. In this directory you find files in the format `YYYY-mm-dd.mdx`. Please find out what the commit date of the last merge base was and then read the changelogs from that date onwards. 
Since you are running as an agent, you need to do the git commands non-interactive.
Please use a worktree to perform the rebase.