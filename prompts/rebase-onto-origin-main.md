We're maintaining a fork of fabro-sh/fabro at fkukuck/fabro. in this fork, we mainly maintain an Azure compatibility. Our fork has diverged from origin/main and we now want to rebase onto it. i've already done `git fetch origin` so main is up to date. i want you to rebase the current branch onto origin/main and resolve all conflicts. moreover, within this process i would like you to check the changes (diffs) the main branch performed since our two branches diverged, and then compare with what we've done in our current checked out Azure branch. if there is anything by now in the origin/main that we originally did ourselves, of course align with the upstream solution. And of course keep existing functionality working under the potentially new way of doing things in the updated branch.
Since you are running as an agent, you need to do the git commands non-interactive.