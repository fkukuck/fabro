---
title: "SSH preview URLs"
date: "2026-03-12"
---

## More

<Accordion title="Improvements">
- SSH sandbox now supports `preview_url_base` — set it to the host's reachable address (e.g. `"http://myserver"`) so port preview URLs resolve correctly instead of falling back to `localhost`
</Accordion>

<Accordion title="Fixes">
- Fixed demo workflow serialization
</Accordion>
